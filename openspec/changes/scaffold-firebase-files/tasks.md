## 1. Templates

- [ ] 1.1 Create `packages/create-app/templates/` and add `firebase.json` (hosting + firestore + emulators per design D2), `.firebaserc` (default alias `your-project-id`), `firestore.rules` (RBAC helper skeleton + deny-all), and `firestore.indexes.json` (`{ "indexes": [] }`)
- [ ] 1.2 Move `packages/create-app/AGENTS.md` to `packages/create-app/templates/AGENTS.md` and update it: add `firebase.json`, `.firebaserc`, `firestore.rules`, `firestore.indexes.json` to the project layout; document that rules/indexes are regenerated by `blaze generate`; note the hosting `public` path and that `.firebaserc` must match the real project id
- [ ] 1.3 Update `packages/create-app/package.json` `files` to `["dist", "bin", "templates"]`

## 2. Scaffold Integration

- [ ] 2.1 In `packages/create-app/src/index.ts`, replace `AGENTS_PATH` with a `readTemplate(name)` helper resolving `new URL("../templates/<name>", import.meta.url)`, and update the `AGENTS.md` call to use it
- [ ] 2.2 Add `createFile` calls for `firebase.json`, `.firebaserc`, `firestore.rules`, and `firestore.indexes.json`, reading each from `templates/`

## 3. Tests

- [ ] 3.1 Extend `packages/create-app/src/__tests__/index.test.ts` "creates all required files" to assert the four new files are written
- [ ] 3.2 Add assertions that `firebase.json` content includes hosting/firestore config and that `.firebaserc` maps `default` to `your-project-id`

## 4. Verification

- [ ] 4.1 Confirm no other references to the old `packages/create-app/AGENTS.md` path remain (e.g. `rg "../AGENTS.md"`)
- [ ] 4.2 Run `pnpm --filter @blazing-cms/create-app typecheck`, `test`, and `build`; all pass
- [ ] 4.3 Run `pnpm --filter @blazing-cms/create-app pack --dry-run` and confirm `templates/` (including `AGENTS.md` and the Firebase files) is in the tarball
- [ ] 4.4 Regenerate a sample project and confirm the four files are created and valid (JSON parses; rules file is valid)
